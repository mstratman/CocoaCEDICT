{
    IBClasses = (
        {CLASS = FirstResponder; LANGUAGE = ObjC; SUPERCLASS = NSObject; }, 
        {
            ACTIONS = {doParse = id; saveAction = id; }; 
            CLASS = "build_dbAppDelegate"; 
            LANGUAGE = ObjC; 
            OUTLETS = {progress = NSProgressIndicator; window = NSWindow; }; 
            SUPERCLASS = NSObject; 
        }
    ); 
    IBVersion = 1; 
}